import styled from "styled-components";

export const StyledSectionContainer = styled(({  ...props }) => <div {...props} />)`
  display:flex;
  max-width: 2040px;
  width: 100%;
  height: auto;
  margin-left: auto;
  margin-right: auto;
`;
